package org.example;

public class DuelManager {
    private ConnectionHandler connectionHandler;

    public DuelManager(ConnectionHandler connectionHandler) {
        this.connectionHandler = connectionHandler;
    }
}
